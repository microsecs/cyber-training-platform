"use client";import{useEffect,useState}from"react";import Link from"next/link";import{createClient}from"@/lib/supabase/client";
export default function Page(){const[x,setX]=useState<any[]>([]);useEffect(()=>{const s=createClient();s.auth.getUser().then(({data:u})=>{if(u.user)s.from("assignments").select("id,status,due_date,courses(id,title,description,duration_minutes),completions(score)").eq("user_id",u.user.id).then(({data})=>setX(data||[]))})},[]);return <main className="mx-auto max-w-6xl px-6 py-10"><div className="text-sm text-cyan-300">My Training</div><h1 className="text-4xl font-bold">Assigned Courses</h1><div className="mt-8 space-y-4">{x.length===0?<div className="rounded-2xl bg-slate-900 p-6">No training assigned.</div>:x.map(a=>{const c=Array.isArray(a.courses)?a.courses[0]:a.courses,cp=Array.isArray(a.completions)?a.completions[0]:a.completions;return <div key={a.id} className="rounded-2xl border border-white/10 bg-slate-900 p-6"><h2 className="text-xl font-semibold">{c?.title}</h2><p className="mt-2 text-slate-400">{c?.description}</p><div className="mt-4">{a.status==="completed"?<span className="text-emerald-300">Completed • {cp?.score}%</span>:<Link href={`/course/${a.id}`} className="rounded-lg bg-cyan-400 px-4 py-3 font-semibold text-slate-950">Start Training</Link>}</div></div>})}</div></main>}